
# main backend deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: consultout-backend
  labels:
    app: consultout-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: consultout-backend
  template:
    metadata:
      labels:
        app: consultout-backend
    spec:
      containers:
      - name: consultout-backend
        image: nchiacedrick/my-app:8b60930b87f992ead88624ff8094186884a50f70
        ports:
        - containerPort: 8080
        envFrom:
        - secretRef:
            name: consultout-backend-secret




           